export default function Prac() {
  const helloStr = "Hello this is my first Practice";

  return (
    <div
      style={{ marginTop: "32px", backgroundColor: "pink" }}
      onClick={() => {
        alert = "클릭";
      }}
    >
      {helloStr}
    </div>
  );
}
